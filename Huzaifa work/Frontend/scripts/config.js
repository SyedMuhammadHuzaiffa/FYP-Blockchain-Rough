// Frontend runtime config
// Set API_BASE to your backend URL to switch from mocks (data.js) to real APIs.
window.CONFIG = {
  API_BASE: "", // e.g., "https://api.example.com"
  // Optional: block explorer base to build transaction links from a tx hash
  CHAIN_EXPLORER_BASE: "https://amoy.polygonscan.com/tx/" // change if using a different chain
};